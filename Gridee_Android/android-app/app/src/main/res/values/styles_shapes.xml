<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ShapeAppearance.Gridee.Pill" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">28dp</item>
    </style>

    <!-- 6 dp rounded icon asset on the native ad's scrim. A shape appearance rather than
         android:clipToOutline, which only takes effect on API 31+ against a minSdk of 24. -->
    <style name="ShapeAppearance.Gridee.AdIcon" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">6dp</item>
    </style>
</resources>
