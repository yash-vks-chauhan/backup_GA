<?xml version="1.0" encoding="utf-8"?>
<!-- Subtle green glow for success state - barely visible, premium feel -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Success glow layer -->
    <item
        android:width="34dp"
        android:height="34dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/scanner_v2_glow_success" />
        </shape>
    </item>
    
    <!-- White hairline stays visible -->
    <item
        android:width="32dp"
        android:height="1.5dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/scanner_v2_white_85" />
        </shape>
    </item>
</layer-list>
