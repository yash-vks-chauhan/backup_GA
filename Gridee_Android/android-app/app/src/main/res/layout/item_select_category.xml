<?xml version="1.0" encoding="utf-8"?>
<!-- One place-category row. Same anatomy as the Profile / Booking History rows —
     44dp circular tile, title, meta line, chevron — but the tile is a live
     SpotDialView showing how many spots are free across the category. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/rowCategory"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_ios_row_press"
        android:clickable="true"
        android:focusable="true"
        android:paddingVertical="16dp"
        android:paddingHorizontal="20dp">

        <com.gridee.parking.ui.views.SpotDialView
            android:id="@+id/dial"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:importantForAccessibility="no" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvCategory"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="College / Campus"
                android:textSize="17sp"
                android:textColor="@color/text_primary"
                android:letterSpacing="-0.01"
                android:fontFamily="@font/inter_semibold"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tvMeta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="9 of 14 spots free · 2 locations"
                android:textSize="13sp"
                android:textColor="@color/text_tertiary"
                android:fontFamily="@font/inter_medium"
                android:layout_marginTop="3dp"
                android:maxLines="1"
                android:ellipsize="end" />

        </LinearLayout>

        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginStart="12dp"
            android:src="@drawable/ic_arrow_right"
            app:tint="@color/text_tertiary"
            android:importantForAccessibility="no" />

    </LinearLayout>

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginStart="80dp"
        android:layout_marginEnd="20dp"
        android:background="@color/divider_hairline" />

</LinearLayout>
