<?xml version="1.0" encoding="utf-8"?>
<!--
  A quiet stop on the bookings rail: label left, when-or-where right.

  Every stop on the rail — travelled, current, skipped, ahead — uses this one grammar.
  The card stages (stage_card_pending / stage_card_active) are the same row with a card
  in place of the two labels, so the gutter geometry is identical throughout.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/stage_row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.gridee.parking.ui.views.StageRailView
        android:id="@+id/stage_rail"
        android:layout_width="20dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/text_stage_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="13dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="10dp"
        android:ellipsize="end"
        android:fontFamily="@font/inter_semibold"
        android:letterSpacing="-0.005"
        android:singleLine="true"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        app:layout_constraintStart_toEndOf="@id/stage_rail"
        app:layout_constraintEnd_toStartOf="@id/text_stage_value"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Checked in" />

    <TextView
        android:id="@+id/text_stage_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="2dp"
        android:fontFamily="@font/inter_medium"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBaseline_toBaselineOf="@id/text_stage_label"
        tools:text="On arrival" />

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/stage_row_bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="bottom"
        app:constraint_referenced_ids="text_stage_label,text_stage_value" />

    <Space
        android:layout_width="1dp"
        android:layout_height="16dp"
        app:layout_constraintTop_toBottomOf="@id/stage_row_bottom"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
